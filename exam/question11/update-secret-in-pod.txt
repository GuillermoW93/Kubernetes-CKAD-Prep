apiVersion: v1
kind: Pod
metadata:
  name: mypod
  namespace: session283884
spec:
  containers:
  - name: mypod
    image: busybox
    volumeMounts:
    - name: secret
      mountPath: "/etc/"
      readOnly: true
  volumes:
  - name: secret
    secret:
      secretName: db-pass

k get secret -n session283884 -o jsonpath={.items[*].data.password} | base64 -d
controlplane:~$ k create secret generic -n session283884 db-pass --from-literal=password=oldpass
